# Files copied on `bos init` but never overwritten on `bos sync`.
# These are "user-owned" — the project customizes them after init.

# Agent guides — project customizes these for its own context
AGENTS.md

# UI routes — project-specific pages and layouts
ui/src/routes/**

# API contract and entry point — project defines its own API
api/src/contract.ts
api/src/index.ts

# API database schema — project defines its own data model
api/src/db/schema.ts

# Generated contract bridge — always regenerated by sync:api-contract
ui/src/api-contract.gen.ts
