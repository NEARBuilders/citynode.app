{
  "extends": "../../bos.config.json",
  "app": {
    "api": {
      "name": "registry",
      "development": "local:.",
      "variables": {
        "registryNamespace": "dev.everything.near"
      }
    }
  }
}
