# Implementation Plans

Generated by the improve skill on 2026-09-15 (post Effect 4 + oRPC v2 migration survey). Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | buildScoped helper + PluginEnv alias + doc rewrite | P1 | S | — | DONE — effect-native drift: 4 of the 7 sites were already migrated to handler-time injection; helper adopted at the remaining 3 (api, auth, bos dev services) |
| 002 | effect-bridge dedupe (runEffect/flattenError ×5) | P1 | S | — | DONE — superseded: the effect-native migration deleted the runEffect bridges entirely |
| 003 | Restore dead SRI integrity fetch hook | P1 | S | — | DONE — `verifySriForUrl` lives in `everything-dev/integrity` and is used by `host/src/services/federation.server.ts` |
| 005 | UI cast fixes (rpcBase, DAO args) | P2 | S | — | DONE — amended post-022: the rpcBase cast moved to everything-dev/ui/api; markdown.tsx `as any` kept (upstream rehype-sanitize typing gap) |
| 006 | Stale surface purge (dead exports, no-op codemod, dead docs) | P2 | S | — | DONE — generated child AGENTS.md replaced with DAO-owned tenant flow; dist-import rewrite restored + fixture-tested |
| 016 | Shared Effect test helpers + port 0 | P2 | S-M | 001 | TODO |
| 004 | oRPC boundary typing (as-any chain, config generics, api inline type) | P1 | M | 001 | TODO |
| 007 | createAuthMiddleware consolidation | P2 | M | 002 | TODO |
| 009 | Error taxonomy + createRpcHandlerOptions | P2 | M | 002, 004 | TODO |
| 011 | CLI flag metadata + per-command help | P2 | M | — | TODO |
| 012 | Codemod hardening (offset bug, one-shot gate) | P2 | S-M | 001 (soft) | TODO |
| 013 | Doctor staleness checks (dist + nested node_modules) | P1 | M | — | TODO |
| 015 | Orchestrator/signal/preflight characterization tests | P2 | M | — | TODO |
| 008 | db/migrate.ts consolidation + savepoint regression fix (+ shared driver consolidation folded in, v2 revision) | P1 | M | — foundation for 017 | DONE — v2 executed on `improve/008-migrate-consolidation`: shared `runMigrations` runner (api base, `while:` retry fix) + shared `createDatabaseDriver` in everything-dev/db; five thin sync-propagated adapters; `_template` fully aligned (migrator.ts → migrate.ts, canonical layer, standard journal with preflight auto-adoption, `TemplateDatabase` alias dropped); `adoptPublicTables` dropped by decision (boot-time table relocation contradicts fail-closed drift handling; live DB has zero public tables); Effect-4 note: `Exit.match` callbacks receive `Cause` — loader uses `Effect.option` instead; sync discovery: api's `db/{index,layer,migrate}.ts` are the verbatim sync source for plugins (D4 full sync exit still 017) |
| 010 | Shared-deps unification (runtime + build) | P3 | M | 006 | DONE — superseded: MF shared-dep version guardrails (citynode.app#106) single-source the effect-critical set from `every-plugin/src/build/shared-deps.ts` across bundle/build/runtime/CLI. Residual note: `host/rsbuild.config.ts` merge semantics with `bos.config.json` shared maps remain a separate concern (see "Findings considered and rejected"). |
| 014 | Init extends-chain caching | P3 | M | — | TODO |
| 017 | Database layer in everything-dev/db — helpers, workspace adoption, sync exit (ticket #89, amended) | P1 | M | 008; effect-native Phase 4 (soft) | TODO |
| 020 | Minimal scaffold as default `bos init` path (ticket #90) | P2 | M | 014 | TODO |
| 022 | Framework ui client factories (`everything-dev/ui/api` + `ui/auth`) | P1 | M | 021 | DONE — deviations from the plan recorded in docs/adr/0006 (phantom-typed factories, endpoint-keyed singleton, non-generic auth client) |
| 021 | Land the open PR train (#119 → #120 → #121) with pre-merge hardening | P1 | M | — | DONE — PRs #119/#120/#121 merged to `main` by the operator; all six defect fixes applied on `main` post-merge (handoff race, API key in URL, dummy signing strategy, duplicated bos-config walk, `bosConfigPath ?? "."`, graft mutation) with new unit tests; full gate green |
| 023 | Typed mount contract — `defineUiPlugin` with a `MountId` union | P1 | S | 021 | DONE — deviations: runtime validation covers `_`-root children only (non-`_` roots never graft, unchanged); `MountId`/`MOUNTS`/`MOUNT_ALIASES` consolidated in mount-registry.ts (derived from `MOUNT_REGISTRY` via `satisfies`) and re-exported from types.ts; registry version bumped to 2026-09-19.1. Note for plan 024: `plugins/_template` has no ui tree post-#121 — nothing to migrate there yet |
| 029 | Platform CDN provider — `deploy.cdn: "platform"` (no-account deploys) | P1 | M | 021 | DONE — oRPC contract routes (`POST /storage/bundles`, `GET /bundles/{account}/{gateway}/{workspace}/{+path}`) + host mount; deviations recorded in docs/adr/0007 (file transport via oRPC contract, not servicesTag host routes); live dev-stack E2E (`bos publish --deploy --cdn platform` + `bos mf check`) left for the operator |
| 033 | `manifest-compose` prototype — manifests → host-built tree, gated before 034 | P1 | M | — | TODO — decisions in ADR 0007/0008; PR #134's composed-SSR failure chain (graftCopy, dev SSR services) motivated re-architecting rather than hardening; prototype is throwaway, `shared/` types + gate evidence survive |
| 034 | Manifest composition rework — `feat/auth-ui-plugin` lands manifests, deletes grafting 100% | P1 | L | 033 (all gates) | TODO — merges the previously sketched "trim" (no interim landable unit: dropping grafting leaves no composed-SSR interim); keep/revert inventory in the plan |
| 035 | Derived OpenAPI mounts — plugins declare HTTP prefixes, host stops hardcoding them | P3 | S-M | 029 | TODO — renumbered from branch-plan 033 (ID taken by the manifest-compose prototype) |
| 036 | Port leases and the scoped stack — block allocation, ownership probes, escalating teardown | P1 | L | quiet-dev-session tickets 01–03 (02/03 for dev-session/dev-view overlap; Phase 0 folds in the 015 subset protecting in-scope files) | TODO — implements ADR 0012; shared-plugin lease broker deliberately deferred to a follow-up plan; **amended 2026-09-24 from the 037/038 audit: destructive materializer + registry lock detail folded into Phase 2, AGENTS.md/dev-workflow docs ride-along added to Phase 4; executes AFTER plan 037** |
| 037 | Dev-session stabilization — quit works at every phase, teardown never orphans, TUI never garbles | P1 (Phase 1 = hotfix) | M | none; executes BEFORE 036 Phase 0 | TODO — from the 2026-09-24 CLI audit; Phase 1 hotfixes the live repaint-residue/alt-screen regressions |
| 038 | Env & port truth — real preflight, stale-proof CORS/BASE_URL, DB ports stop resetting | P1 | M | 037 (soft) | TODO — from the audit; carries verified fixes for the live `CORS_ORIGIN 3000 vs 3004` incident class |
| 039 | Security hardening — fail-closed SRI, validated plugin keys, TLS/key/log hygiene | P1 | M | none (coordinate with 038 on env files) | TODO — from the audit |
| 040 | Verification & speed — root chain runs the framework suite, no network on boot/test paths | P1 | M | none | TODO — from the audit |
| 041 | CLI hygiene batch — dead deps, zod as dependency, no `~orpc` cast, doc truth | P2 | S | none | TODO — from the audit |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **024–026 supersedence note (2026-09-21):** plan 024's implementation (PR #134) exposed composed-SSR grafting as a structural failure class. ADR 0007/0008 re-architect: plan 025 executes **manifest-first** (second consumer of the 034 machinery), plan 026's "shell into host" simplifies (host constructs all routes — one construction story), and plan 024's auth-ui carve-out content is absorbed into 034's rework on the same branch. Read ADRs 0007/0008 before any of 025/026.
- **033 gates 034; 034 supersedes the graft protocol entirely** — plans referencing grafting (`defineUiPlugin`, `./tree`, `graftCopy`, mount v1 semantics) execute only as far as 034's keep/revert inventory says.

- 004 requires 001 — both rewrite `packages/every-plugin/src/plugin.ts` and `api/src/index.ts`; land 001 first so the typing work sees the final call-site shape.
- 007 requires 002 — both rewrite the template's `lib/` directory.
- 009 requires 002 + 004 — the unwrapper/interceptor consolidation sits on code both plans reshape.
- 016 requires 001 — `runWithLayer` builds on `buildScopedContext`.
- 010 requires 006 — plan 006 deletes dead exports from the same `shared-deps.ts`.
- 012 pairs with 015's test-first ethos but has no hard file overlap; its interleaved fixture is written inside the plan.
- 008/012/015 are the riskiest (migration money-path, user-source rewriting, process lifecycle) — their plans are test-first by construction; do not reorder their internal steps.
- 017 executes the amended [#89 design doc](../plans/v1-current/every-plugin-db-auth-absorption.md): `everything-dev/db` gains `createDatabaseDriver`/`databaseLayer`/`pluginSchemaName` and consumes 008's runner; db files and `lib/context.ts` exit sync ownership. The original 017–019 trio (every-plugin/db + /auth facades) was rejected in the 2026-09-15 amendment — every-plugin's surface is shrinking (effect-native Phase 5 removes its barrels), everything-dev/db is already the runtime-imported home, and alchemy (decisions 13/16) makes the deploy package the long-term migration owner. Plan 007's auth convergence is the resting point. 020 records the #90 decision and builds on 014's `ResolvedChainContext`.
- Waves: {001, 005, 006} are independent quick wins (002 and 010 are marked DONE above — superseded by the effect-native migration and the MF shared-dep guardrails respectively); {004, 007, 009, 011, 012, 013, 015, 016} next; {008,  014} last. 007's dependency on 002 is void (002 is superseded); it now depends only on its own file shape.

## Verification gates common to every plan

- `bun typecheck` (all workspaces; also regenerates gitignored `.gen.ts` type files)
- `bun lint` (biome, must exit 0)
- Per-workspace suites per each plan's command table
- After touching `packages/every-plugin` or `packages/everything-dev`: `bun run build` in that package before integration tests / CLI smoke (stale-dist is a known crash class; plan 013 adds automated detection)
- Host-touching plans (003, 004, 009): `bun run --cwd host test` — 2 `runtime-remote` failures are known deploy-gated (production v1 remotes vs v2 host); they pass after `bos publish --deploy`. Never chase them in these plans.

## Findings considered and rejected

### From the 2026-09-24 CLI audit (plans 037–041; full finding register in the audit session, key rejections/deferrals here)

- **Architecture cluster — DEFERRED by operator choice**: plugin.ts (2,197-line god module) / cli.ts (1,100-line main, `any` client boundary) split; cli/↔infra/ layering inversion; four logging systems; `pendingSession` module globals; non-interactive view duplication. All HIGH-confidence, M-effort — schedule after 036/037 settle the dev-session shape.
- **DEP-04** (@clack 0.10→1.8, chalk 5→6, execa 9→10): coordinated `engines` decision, not batchable.
- **SEC-09** (PID registry same-user trust): investigate-only — marginal risk over direct same-user signaling.
- **PERF-02** (every command eagerly resolves full config incl. remote manifests) and **PERF-07** (host spawn gated on slowest plugin): real, need design decisions — next audit.
- **TEST-04** (skipped dist-CLI publish smoke): follow-up recorded in 040.
- **BUG-13** (display-width-aware clipping for emoji/wide glyphs): deferred in 037 maintenance notes (needs wcwidth-style table).
- **DIR-03** (`bos logs` level parity — classifier output dropped at the file boundary): design spike, not scheduled.
- **DIR-04** (workspace-orchestrator seam `BOS_WORKSPACE_CHILD`/`portBudget`/reserved registry roles): build-or-remove decision ticket AFTER 036 Phase 2.
- **CORRECTNESS-08** (AGENTS.md "inherit and override" vs plugins-map replace-on-declare, `merge.ts:101-105`): decision drift — doc fix or merge change is an operator call; tests pin replace.
- **BUG-10** (log-file writes not drained before `process.exit`): companion work noted in 037.
- **Non-findings verified**: `bun audit` advisories all outside this package's paths; `.bos/session.json` already 0600 + gitignored; tar extraction traversal-safe; login callback protected by 128-bit state; `normalizeSlug` prevents SQL interpolation; no prompt-injection content in the audited tree; no secret values reproduced in any plan.

### From the 2026-09-15 survey (original run)

- `module-federation.service.ts` remote-constructor `any`s (~lines 144-182): genuine untrusted-module boundary with explicit error guards — not worth typing.
- Dev-server CORS `*`: dev-only local surface, standard convention.
- `secrets.service.ts` regex interpolation of secret keys: keys are host-controlled config, not request data; would become a finding if `SecretsConfig` ever becomes user-writable.
- `ui/src/routeTree.gen.ts` casts: generated file.
- Vendored `packages/better-near-auth` test `any`s: test-only mocks in a vendored package.
- Full error-taxonomy merge (host `FederationError` ≡ every-plugin `ModuleFederationError`): host tags are load-bearing in SSR tests/logs; only the unwrappers and interceptor options are consolidated (plan 009). Revisit with a log-consumer inventory.
- Consolidating `host/rsbuild.config.ts`'s shared-deps copy into every-plugin (plan 010 keeps it out of scope): different merge semantics with bos.config shared maps; recorded as deferred follow-up in plan 010's maintenance notes.

## Related tickets (decision status)

- [#89](https://github.com/NEARBuilders/citynode.app/issues/89) — RESOLVED (spike, **amended 2026-09-15**): [design doc](../plans/v1-current/every-plugin-db-auth-absorption.md) + [prototype](../plans/prototypes/db-auth-absorption/); amended decision — home is `everything-dev/db` (`createDatabaseDriver`/`databaseLayer`/`runMigrations`), no `every-plugin/db` or `every-plugin/auth`, plugin-owned typed tags, db files exit sync ownership; build plan 017, depends on 008, rides effect-native Phase 4.
- [#90](https://github.com/NEARBuilders/citynode.app/issues/90) — RESOLVED (decision): minimal scaffold by default + sparse fetch of vendored workspaces, `--full` opt-out; build plan 020, builds on plan 014's `ResolvedChainContext`.
- [#91](https://github.com/NEARBuilders/citynode.app/issues/91) — RESOLVED (decision + implemented): Option A with `Config.redacted` — all three host env-read sites on the Effect 4 Config module, secrets/DB URLs redacted with explicit unredaction at the `usePlugin` boundary, shared `maskDbUrl()` dedupe.
