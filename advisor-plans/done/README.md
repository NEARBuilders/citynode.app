# Archived plans

Closed advisor plans — executed (DONE), superseded, or absorbed. The status
notes below are preserved from the execution history; the plans themselves
are the detailed record. Open work lives in [../README.md](../README.md).

| Plan | Title | Outcome |
|------|-------|---------|
| [001](./001-buildscoped-helper.md) | buildScoped helper + PluginEnv alias + doc rewrite | DONE — effect-native drift: 4 of the 7 sites were already migrated to handler-time injection; helper adopted at the remaining 3 (api, auth, bos dev services) |
| [002](./002-effect-bridge-dedupe.md) | effect-bridge dedupe (runEffect/flattenError ×5) | DONE — superseded: the effect-native migration deleted the runEffect bridges entirely |
| [003](./003-sri-integrity-hook.md) | Restore dead SRI integrity fetch hook | DONE — `verifySriForUrl` lives in `everything-dev/integrity` and is used by `host/src/services/federation.server.ts` |
| [005](./005-ui-cast-fixes.md) | UI cast fixes (rpcBase, DAO args) | DONE — amended post-022: the rpcBase cast moved to everything-dev/ui/api; markdown.tsx `as any` kept (upstream rehype-sanitize typing gap) |
| [006](./006-stale-surface-purge.md) | Stale surface purge (dead exports, no-op codemod, dead docs) | DONE — generated child AGENTS.md replaced with DAO-owned tenant flow; dist-import rewrite restored + fixture-tested |
| [008](./008-migrate-consolidation.md) | db/migrate.ts consolidation + savepoint regression fix (+ shared driver consolidation folded in, v2 revision) | DONE — v2 executed on `improve/008-migrate-consolidation`: shared `runMigrations` runner (api base, `while:` retry fix) + shared `createDatabaseDriver` in everything-dev/db; five thin sync-propagated adapters; `_template` fully aligned (migrator.ts → migrate.ts, canonical layer, standard journal with preflight auto-adoption, `TemplateDatabase` alias dropped); `adoptPublicTables` dropped by decision (boot-time table relocation contradicts fail-closed drift handling; live DB has zero public tables); Effect-4 note: `Exit.match` callbacks receive `Cause` — loader uses `Effect.option` instead; sync discovery: api's `db/{index,layer,migrate}.ts` are the verbatim sync source for plugins (D4 full sync exit still 017) |
| [010](./010-shared-deps-unification.md) | Shared-deps unification (runtime + build) | DONE — superseded: MF shared-dep version guardrails (citynode.app#106) single-source the effect-critical set from `every-plugin/src/build/shared-deps.ts` across bundle/build/runtime/CLI. Residual note: `host/rsbuild.config.ts` merge semantics with `bos.config.json` shared maps remain a separate concern (see plan's "Findings considered and rejected") |
| [021](./021-land-open-train.md) | Land the open PR train (#119 → #120 → #121) with pre-merge hardening | DONE — PRs #119/#120/#121 merged to `main` by the operator; all six defect fixes applied on `main` post-merge (handoff race, API key in URL, dummy signing strategy, duplicated bos-config walk, `bosConfigPath ?? "."`, graft mutation) with new unit tests; full gate green |
| [022](./022-framework-ui-client.md) | Framework ui client factories (`everything-dev/ui/api` + `ui/auth`) | DONE — deviations from the plan recorded in docs/adr/0006 (phantom-typed factories, endpoint-keyed singleton, non-generic auth client) |
| [023](./023-typed-mount-contract.md) | Typed mount contract — `defineUiPlugin` with a `MountId` union | DONE — deviations: runtime validation covers `_`-root children only (non-`_` roots never graft, unchanged); `MountId`/`MOUNTS`/`MOUNT_ALIASES` consolidated in mount-registry.ts (derived from `MOUNT_REGISTRY` via `satisfies`) and re-exported from types.ts; registry version bumped to 2026-09-19.1 |
| [029](./029-platform-cdn.md) | Platform CDN provider — `deploy.cdn: "platform"` (no-account deploys) | DONE-SUPERSEDED (ADR 0011, pre-release) — the DB-backed storage + CLI upload flow shipped in #133 but never released; plan 043 replaces it with image-native FS serving; the E2E harness + cache/traversal learnings carry over |
| [033](./033-manifest-compose-prototype.md) | `manifest-compose` prototype — manifests → host-built tree, gated before 034 | DONE — prototype gate evidence consumed by ADR 0008 §7 (hydration e2e); `shared/` types + gate evidence survive in `plans/prototypes/manifest-compose/`; 034's rework cites the prototype as prior art |
| [034](./034-manifest-composition-rework.md) | Manifest composition rework — `feat/auth-ui-plugin` lands manifests, deletes grafting 100% | DONE — LANDED 2026-09-21 (`feat/auth-ui-plugin`, five commits incl. `a357d0de`): graft machinery, `defineUiPlugin`, `./tree` exposes, v1 registry+digest, dev-SSR machinery deleted 100%; the host builds the graph from manifests through the core's `./compose` engine; `--ssr` = source-manifest compose in-process. Hand-runbook pending at commit time — see the plan's status |
| [024](./024-auth-ui-carve-out.md) | Auth ui carve-out — `plugins/auth/ui` as the first grafted plugin | ABSORBED into 034 — plan 024's implementation (PR #134) exposed composed-SSR grafting as a structural failure class; its auth-ui carve-out content is absorbed into 034's rework on the same branch. See ADR 0007/0008 |
| [025](./025-landing-carve-out.md) | Landing carve-out — second consumer of the grafting machinery | SUPERSEDED — executes manifest-first as the second consumer of the 034 machinery. See ADR 0007/0008 before any successor work |
| [026](./026-shell-into-host.md) | Shell into host — one construction story | SUPERSEDED — the host constructs all routes via manifest composition, which simplifies "shell into host" to the same story. See ADR 0007/0008 |
| [027](./027-db-receipt.md) | DB receipt — `everything-dev/db` runtime layer for plugins | SUPERSEDED — duplicate of plan 017 (same `everything-dev/db` design); 017 in the main README is the live plan |
| [036](./036-port-leases-and-scoped-stack.md) | Port leases and the scoped stack — block allocation, ownership probes, escalating teardown | DONE — `cffa5e8b5` (Phase 1: ownership probe, bos kill escalation + dead-entry childPids adoption + verified release), `3d0e6d2fc` (Phase 2: atomic block allocation + announced drift + persist-only-explicit + lease-shaped registry + deleted the dead divergent allocator and destructive `.env` materializer), `12c4eb47d` (Phase 3: scope-owned child handles), `84857d7b0` (Phase 4 docs ride-along). Acceptance green. Deferred: shared-plugin lease broker (follow-up plan; registry seam landed); strict-ports flag. Known follow-up: phantom typecheck errors while a sibling dev session runs from this repo; `bun run build` clobbers the tsgo patch (run `bun run prepare` after builds, before typecheck) |
| [037](./037-dev-session-stabilization.md) | Dev-session stabilization — quit works at every phase, teardown never orphans, TUI never garbles | DONE — phases 1-3 (`baf470d02`, `c5cd5ff71`, `5a9af4395`): quit escalation + viewport repaint, startup-window quit + no self-SIGKILL + honest exit codes, non-TTY freeze + interactive fallback + post-ready death + detectStatus fixes + per-session logs; characterization in `dev-session.test.ts` |
| [038](./038-env-and-port-truth.md) | Env & port truth — real preflight, stale-proof CORS/BASE_URL, DB ports stop resetting | DONE — `03def1b6d`: preflight parses credentialed URLs (was a silent no-op), BASE_URL/CORS_ORIGIN generated-owned at every tier (shell/.env/process.env/bos start), DB ports honor persisted state; tests in `origin-truth.test.ts` |
| [039](./039-security-hardening.md) | Security hardening — fail-closed SRI, validated plugin keys, TLS/key/log hygiene | DONE — `c10bb0205`: plugin-key validation at the config boundary + no shell spawn, TLS verify-by-default, SRI fail-closed + integrity-never-deleted + hash retry, key file 0600 + non-TTY key warning, masked drift logs |
| [040](./040-verification-and-speed.md) | Verification & speed — root chain runs the framework suite, no network on boot/test paths | DONE — `c724ef3b7`: root `bun run test` includes the framework suites, dev/start spawn unblocked from the outdated check (stderr-routed), config tests stub the network boundary. PERF-05 pool tuning MEASURED AND REJECTED (2026-09-24): wall time already fine with default forks; keep defaults. Deferred: config-test residual ~15s, Step 5 spec-once |
| [041](./041-cli-hygiene-batch.md) | CLI hygiene batch — dead deps, zod as dependency, no `~orpc` cast, doc truth | DONE — `5d74a034a`: dead @effect/platform deps removed, zod as real dependency, @types/node+vitest on catalog, `commandOptionSchemas` static map (no `~orpc` cast), AGENTS.md bos info fix, hash/SQLSTATE/near-cli dedupes |
| [042](./042-plugin-watcher-kill-escalation.md) | Plugin watcher kill escalation — rspack/rsbuild watchers can never outlive their dev server | DONE — `watch-kill.ts` (SIGTERM→2s→SIGKILL escalation + ppid parent-death supervision) wired into `serve.ts close()`; from the 2026-09-24 orphan incident (15 PPID-1 rspack watchers); carries an amendment to 036 Phase 1.4. (042-spreadability — the other 042 — stays open in the main README) |
| [043](./043-image-native-artifacts.md) | Image-native artifacts — namespace-as-topology, FS-backed serving, no shared storage | PHASES A+B SHIPPED — A: FS-backed `/bundles/*` (`BOS_BUNDLE_DIR`), namespace staging + Dockerfile prod stage, credential-free publish; B: sunset — `bundle_objects` + upload/serve routes deleted (migration squashed), Zephyr + `deploy.cdn`/`--cdn` removed, publish unconditionally writes deterministic URLs, `bos plugin publish` writes the image-native URL, mf gate reintroduced. Phase C (tenant break-off runbook) TODO — ADR 0011 |

Renumbering note: the former second-template files 025-wallet-invitation-network,
026-wallet-invitation-lifecycle, 027-workspace-synchronization,
028-shared-team-access-policy and 029-team-membership-loading were renumbered to
044–048 in the main README (all DONE via PR #136).
