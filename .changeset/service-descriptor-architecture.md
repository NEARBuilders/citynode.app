---
"everything-dev": minor
"@everything-dev/auth-plugin": minor
"every-plugin": patch
"ui": patch
"host": patch
"api": patch
"@every-plugin/template": patch
"@everything-dev/opencode-plugin": patch
"@everything-dev/projects-plugin": patch
"@everything-dev/registry-plugin": patch
---

Refactor dev orchestrator to service-descriptor architecture; add NEAR auth contract routes (nonce, verify, profile, relay, view); consolidate session queries in UI; add source-map devtool for plugin builds
