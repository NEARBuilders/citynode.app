---
"ui": patch
---

Remove header branding from public and anon layouts — the public header now shows only the user nav. Point the Built on NEAR footer badge to nearbuilders.org. Rename the unused BrandElement component to Logo (city icon) for future reuse.
