---
"ui": patch
---

Make tenant deployment follow the active NEAR network and configured subaccount parent, use the connected wallet's public key for subaccount creation, and pass the testnet parent key into the auth runtime.
