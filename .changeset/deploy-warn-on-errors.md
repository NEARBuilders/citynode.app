---
"everything-dev": patch
---

Warn when `[BOS_DEPLOY]` lines are present but rspack exited with errors. Add `DrizzleORMMigrations` plugin and `pg`/`@electric-sql/pglite` externals to plugin rspack configs by default.
