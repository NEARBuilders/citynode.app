---
"everything-dev": patch
---

Fix init template: add missing `postcss.config.mjs`, `ui/src/assets/**`, integrations, and `_authenticated` route children; remove phantom entries; add `api-contract.gen.ts` stub generation; strip `development` exports from published every-plugin; align `zod` dependency to `^4.3.6` to match every-plugin shared scope
