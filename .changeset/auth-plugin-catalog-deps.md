---
"@everything-dev/auth-plugin": patch
---

Consume `better-near-auth` from the workspace via `catalog:` and align remaining hard-coded dependency versions (`drizzle-orm`, `pg`, `@electric-sql/pglite`, `@orpc/contract`, `@orpc/server`) with the root catalog.
