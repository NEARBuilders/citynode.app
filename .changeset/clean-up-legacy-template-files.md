---
"everything-dev": patch
---

Remove `.templatekeep` and `.templatesync-exclude` during `bos upgrade` — these files belong to the deprecated template sync pattern that has been replaced by `bos sync`.