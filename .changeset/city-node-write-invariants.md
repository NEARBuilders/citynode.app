---
"api": patch
---

Reject cyclic City Node reparenting, bound recursive hierarchy reads when existing records contain cycles, and preserve the current default validator when a replacement write fails.
