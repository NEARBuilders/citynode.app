/**
 * Re-export shim — the UI rsbuild factory lives in
 * `every-plugin/ui/mf-build` (ADR 0008). Consumed via the stable
 * `everything-dev/ui/mf-build` specifier.
 */

export * from "every-plugin/ui/mf-build";
