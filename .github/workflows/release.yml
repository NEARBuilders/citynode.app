name: Release

on:
  push:
    branches:
      - main
    paths:
      - ".changeset/**"
      - "package.json"
      - ".github/workflows/release.yml"
  workflow_dispatch:

concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      actions: write
    outputs:
      hasChangesets: ${{ steps.changesets.outputs.hasChangesets }}
    env:
      ZE_SECRET_TOKEN: ${{ secrets.ZEPHYR_AUTH_TOKEN }}
      ZE_SERVER_TOKEN: ${{ secrets.ZEPHYR_SERVER_AUTH_TOKEN }}
      ZE_USER_EMAIL: ${{ secrets.ZEPHYR_USER_EMAIL }}
      BOS_INSTALL_NEAR_CLI: "true"
      NEAR_PRIVATE_KEY: ${{ secrets.NEAR_PRIVATE_KEY }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.2.20"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Create Release Pull Request or Publish
        id: changesets
        uses: changesets/action@v1
        with:
          version: bun run version
          publish: bun run release
          title: "chore: version packages"
          commit: "chore: version packages"
          createGithubReleases: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create GitHub Releases
        if: steps.changesets.outputs.hasChangesets == 'false'
        run: |
          # Collect packages from bos.config.json (app keys + plugin keys) plus framework packages
          APP_KEYS=$(jq -r '.app | keys[]' bos.config.json)
          PLUGIN_KEYS=$(jq -r '.plugins | keys[]' bos.config.json 2>/dev/null || true)
          FRAMEWORK_PKGS="packages/every-plugin packages/everything-dev"
          
          ALL_KEYS=""
          for key in $APP_KEYS $PLUGIN_KEYS; do
            ALL_KEYS="$ALL_KEYS $key"
          done
          
          for pkg_dir in $ALL_KEYS $FRAMEWORK_PKGS; do
            PKG_NAME=$(basename "$pkg_dir")
            PKG_JSON="${pkg_dir}/package.json"
            
            if [ ! -f "$PKG_JSON" ]; then
              echo "Skipping $pkg_dir — no package.json"
              continue
            fi
            
            VERSION=$(jq -r '.version' "$PKG_JSON")
            NAME=$(jq -r '.name' "$PKG_JSON")
            TAG="${NAME}@${VERSION}"
            
            if ! gh release view "$TAG" &>/dev/null; then
              echo "Creating release for $TAG"
              
              CHANGELOG="${pkg_dir}/CHANGELOG.md"
              NOTES=""
              if [ -f "$CHANGELOG" ]; then
                NOTES=$(sed -n "/^## ${VERSION}/,/^## [0-9]/p" "$CHANGELOG" | sed '$d' | tail -n +2)
              fi
              
              if [ -z "$NOTES" ]; then
                NOTES="Release $TAG"
              fi
              
              gh release create "$TAG" \
                --title "$TAG" \
                --notes "$NOTES" \
                --target main
            else
              echo "Release $TAG already exists, skipping"
            fi
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Build everything-dev
        if: steps.changesets.outputs.hasChangesets == 'false'
        run: bun run --cwd packages/everything-dev build

      - name: Publish runtime config
        if: steps.changesets.outputs.hasChangesets == 'false'
        run: bun packages/everything-dev/cli.js publish --deploy

      - name: Commit bos.config.json updates
        if: steps.changesets.outputs.hasChangesets == 'false'
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "chore: update deployment URLs [skip ci]"
          file_pattern: bos.config.json

      - name: Trigger Docker deployment
        if: steps.changesets.outputs.hasChangesets == 'false'
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'docker.yml',
              ref: 'main'
            });

  publish-npm:
    name: Publish to npm
    needs: release
    if: ${{ needs.release.outputs.hasChangesets == 'false' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: "1.2.20"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          registry-url: "https://registry.npmjs.org"

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build every-plugin
        run: bun run --cwd packages/every-plugin build

      - name: Build everything-dev
        run: bun run --cwd packages/everything-dev build

      - name: Publish every-plugin
        run: npm publish --workspace packages/every-plugin --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish everything-dev
        run: npm publish --workspace packages/everything-dev --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}